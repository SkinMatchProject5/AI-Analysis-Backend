# OpenAI API 키 (실제 키로 교체하세요)
# https://platform.openai.com/account/api-keys 에서 발급 가능
OPENAI_API_KEY=your_openai_api_key_here

# 환경 설정
ENVIRONMENT=development

# 로그 레벨 (debug, info, warning, error)
LOG_LEVEL=info

# 파이프라인/프로바이더 설정
# 증상 문장 다듬기 제공자 (현재 openai만 지원)
SYMPTOM_REFINER_PROVIDER=openai
SYMPTOM_REFINER_MODEL=gpt-4o-mini

# 의학적 해석 제공자 (openai | runpod)
INTERPRETATION_PROVIDER=openai
INTERPRETATION_MODEL=gpt-4o-mini

# RunPod 사용 시 설정 (선택)
# RUNPOD_ENDPOINT_URL=https://api.runpod.io/your-endpoint
# RUNPOD_API_KEY=your_runpod_api_key
# RUNPOD_MODEL_ID=your_model_id
